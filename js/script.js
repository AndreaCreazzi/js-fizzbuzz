// Controllo JS
console.log(`JS OK`);

// Dati preliminari
const gameElement = document.getElementById(`game`)

let numbers = `100`
let text = ``

// Dati concreti
for(let i = 1 ; i <= numbers ; i++){
    console.log(i)
};